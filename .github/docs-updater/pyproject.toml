[project]
name = "laravel-docs-updater"
version = "0.1.0"
description = "GitHub Actions updater for Laravel Korean documentation"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0,<2",
    "python-dotenv>=1.0,<2",
]

[dependency-groups]
dev = [
    "pytest>=9.0,<10",
]
