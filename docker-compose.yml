services:
  translate:
    build:
      context: .
      dockerfile: Dockerfile.translate
    env_file:
      - path: .github/docs-updater/.env
        required: false
    volumes:
      - ./versioned_docs:/app/versioned_docs
      - ./versioned_sidebars:/app/versioned_sidebars
